
<%= form_with model:[post, new_comment] do |form|%>

    <div class="form-comments-main">
        <div class="form-comments-main-name">
            <%= form.text_field :guest_name, placeholder: "Enter your name"%>
        </div>
        <div class="form-comments-main-body">
            <%= form.text_area :body, placeholder: "Write your comment"%>
        </div>
    </div>

    <div class="form-comments-actions">
        <%= form.submit "Submit", class: "form-comments-actions-button"%>
    </div>

<% end %>
